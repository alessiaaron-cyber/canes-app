window.CR = window.CR || {};
(() => {
  const CR = window.CR;
  const roster=[{name:'Sebastian Aho',detail:'C • Top line'},{name:'Andrei Svechnikov',detail:'RW • PP1'},{name:'Seth Jarvis',detail:'RW • Hot streak'},{name:'Jaccob Slavin',detail:'D • Defensive anchor'},{name:'Jordan Staal',detail:'C • Two-way center'},{name:'Jesperi Kotkaniemi',detail:'C • Middle six'},{name:'Brent Burns',detail:'D • PP2'},{name:'Jackson Blake',detail:'RW • Rookie spark'}];
  const draftOrder=['Aaron','Julie','Aaron','Julie'];
  CR.gameDay={mode:'pregame',playoffMode:'regular',pregame:{Aaron:['Sebastian Aho','Andrei Svechnikov'],Julie:['Seth Jarvis','Jaccob Slavin']},live:{scores:{Aaron:4,Julie:3},period:'2nd • 8:14',users:{Aaron:[{player:'Sebastian Aho',goals:1,assists:0,firstGoal:true},{player:'Andrei Svechnikov',goals:0,assists:1,firstGoal:false}],Julie:[{player:'Seth Jarvis',goals:0,assists:1,firstGoal:false},{player:'Jaccob Slavin',goals:0,assists:1,firstGoal:false}]},feed:[{icon:'🚨',title:'Sebastian Aho goal',detail:'Aaron scores through a picked player',points:2},{icon:'🍎',title:'Seth Jarvis assist',detail:'Julie adds an assist point',points:1},{icon:'⭐',title:'First goal bonus',detail:'Aho hit the first Canes goal bonus',points:2}]}};
  const $=(s)=>document.querySelector(s); const pointsFor=(p)=>(p.goals*2)+p.assists+(p.firstGoal?2:0); const clone=(x)=>JSON.parse(JSON.stringify(x)); const isPlayoffs=()=>CR.gameDay.playoffMode==='playoffs';
  const getPregameStructured=()=>({Aaron:CR.gameDay.pregame.Aaron.map((player)=>({player})),Julie:CR.gameDay.pregame.Julie.map((player)=>({player}))});
  const getFinalData=()=>({scores:clone(CR.gameDay.live.scores),users:clone(CR.gameDay.live.users)});
  const winnerText=(scores)=>scores.Aaron>scores.Julie?'Aaron Wins':scores.Julie>scores.Aaron?'Julie Wins':'Rivalry Tie';
  const nextDraftSide=()=>{const total=CR.gameDay.pregame.Aaron.length+CR.gameDay.pregame.Julie.length; return total>=4?null:draftOrder[total];};
  const claimedOwner=(name)=>CR.gameDay.pregame.Aaron.includes(name)?'Aaron':CR.gameDay.pregame.Julie.includes(name)?'Julie':'';
  const totalGoals=(users)=>Object.values(users).flat().reduce((n,p)=>n+(p.goals||0),0); const totalAssists=(users)=>Object.values(users).flat().reduce((n,p)=>n+(p.assists||0),0); const firstGoalHit=(users)=>Object.values(users).flat().find((p)=>p.firstGoal);
  const mvpText=(users)=>{const all=Object.values(users).flat(); if(!all.length)return 'No MVP yet'; let best=all[0], bestPts=pointsFor(best); all.forEach((pick)=>{const pts=pointsFor(pick); if(pts>bestPts){best=pick; bestPts=pts;}}); return `${best.player} • ${bestPts} pts`;};
  const leadingStatType=(users)=>{const goals=totalGoals(users), assists=totalAssists(users); if(goals===0&&assists===0)return 'No scoring events yet'; if(goals>assists)return `${goals} goal points drove the night`; if(assists>goals)return `${assists} assist points drove the night`; return 'Goals and assists landed evenly';};
  function setModalOpen(isOpen){const modal=$('#manageSheet'); if(!modal)return; modal.classList.toggle('is-open',isOpen); document.body.style.overflow=isOpen?'hidden':''; document.documentElement.style.overflow=isOpen?'hidden':'';}
  function renderHero(){const mode=CR.gameDay.mode,pregameUsers=getPregameStructured(),live=CR.gameDay.live,final=getFinalData(),pregame=mode==='pregame',liveMode=mode==='live',finalMode=mode==='final',scores=pregame?{Aaron:0,Julie:0}:(finalMode?final.scores:live.scores),period=pregame?(isPlayoffs()?'Playoff Night • 7:00 PM':'Tonight • 7:00 PM'):(liveMode?live.period:''),delta=scores.Aaron-scores.Julie,momentum=Math.min(Math.abs(delta)*12,48),subline=pregame?(nextDraftSide()?`${nextDraftSide()} on the clock • Pick ${pregameUsers.Aaron.length+pregameUsers.Julie.length+1} of 4`:'Picks ready for puck drop'):(liveMode?(isPlayoffs()?'Playoff rivalry scoring live':'Rivalry scoring live'):''); return `<section class="gd-hero ${finalMode?'gd-hero-final':''}"><div class="gd-status-row"><span class="gd-pill ${finalMode?'final':'live'}">${finalMode?'Final':(liveMode?'Live':'Pregame')}</span>${period?`<span class="gd-period">${period}</span>`:''}${liveMode?'<span class="gd-pill synced">Synced</span>':''}${isPlayoffs()?'<span class="gd-pill gd-pill-playoff">Playoffs</span>':''}</div><div class="gd-score-grid"><div class="gd-side"><div class="gd-side-label red">Aaron</div>${pregame?`<div class="gd-pick-meta"><span class="gd-pick-chip ${pregameUsers.Aaron.length===2?'active':''}">${pregameUsers.Aaron.length} of 2 locked</span></div>`:`<div class="gd-side-value">${scores.Aaron}</div>`}</div><div class="gd-center"><img class="gd-logo" src="./assets/app-icon.png" alt="Canes Rivalry"></div><div class="gd-side"><div class="gd-side-label">Julie</div>${pregame?`<div class="gd-pick-meta"><span class="gd-pick-chip ${pregameUsers.Julie.length===2?'active':''}">${pregameUsers.Julie.length} of 2 locked</span></div>`:`<div class="gd-side-value">${scores.Julie}</div>`}</div></div>${subline?`<div class="gd-subline">${subline}</div>`:''}${finalMode?`<div class="gd-final-banner">${winnerText(scores)}</div>`:''}${liveMode?`<div class="gd-momentum-label">Momentum</div><div class="gd-track"><div class="gd-track-fill" style="left:${scores.Aaron>=scores.Julie?'50%':`calc(50% - ${momentum}%)`};width:${momentum}%"></div></div>`:''}</section>`;}
  function renderPregame(){const users=getPregameStructured(); const renderOwner=(side,red)=>`<article class="gd-panel"><div class="gd-panel-head ${red?'red':'dark'}"><span>${side}</span><span>${users[side].length}/2</span></div>${[0,1].map((index)=>{const pick=users[side][index]; return pick?`<div class="gd-pick-row"><div class="gd-pick-icon">✓</div><div class="gd-pick-main"><strong>${pick.player}</strong><small>Locked pick</small><div class="gd-lock-actions"><button class="gd-small-action" data-side="${side}" data-player="${pick.player}" type="button">Change</button></div></div></div>`:`<div class="gd-pick-row is-empty"><div class="gd-pick-icon">…</div><div class="gd-pick-main"><strong>Open slot</strong><small>Waiting for next pick</small></div></div>`;}).join('')}</article>`; return `<div class="gd-label-row"><div class="gd-label">Live Picks</div><div class="gd-filter">Updates instantly</div></div><section class="gd-picks-grid">${renderOwner('Aaron',true)}${renderOwner('Julie',false)}</section><div class="gd-label-row"><div class="gd-label">Current Canes Roster</div><div class="gd-filter">Tap Draft only</div></div><section class="gd-panel gd-roster gd-scroll">${roster.map((entry)=>{const owner=claimedOwner(entry.name); return `<div class="gd-roster-row ${owner?'claimed':''}"><div class="gd-pick-main"><strong>${entry.name}</strong><small>${entry.detail}</small></div>${owner?`<span class="gd-tag">${owner}</span>`:`<button class="gd-draft-btn" data-player="${entry.name}" type="button">Draft</button>`}</div>`;}).join('')}</section>`;}
  const renderStatChips=(pick)=>`<div class="gd-player-stats"><span class="gd-stat ${pick.goals?'live':''}">G ${pick.goals}</span><span class="gd-stat ${pick.assists?'live':''}">A ${pick.assists}</span><span class="gd-stat ${pick.firstGoal?'live':''}">FG</span></div>`;
  const renderPlayerCard=(side,picks,score,red)=>`<article class="gd-card"><div class="gd-pick-card-head"><strong class="${red?'red':''}">${side}</strong><span>${score} pts</span></div>${picks.map((pick)=>`<div class="gd-player-card"><div class="gd-player-main"><strong>${pick.player}</strong>${renderStatChips(pick)}</div><div class="gd-player-total">+${pointsFor(pick)}</div></div>`).join('')}</article>`;
  function renderLive(){const state=CR.gameDay.live; return `<div class="gd-label-row"><div class="gd-label">Picked Players</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-picks-grid">${renderPlayerCard('Aaron',state.users.Aaron,state.scores.Aaron,true)}${renderPlayerCard('Julie',state.users.Julie,state.scores.Julie,false)}</section><div class="gd-label-row"><div class="gd-label">Simulate Updates</div><div class="gd-filter">Goal / Assist / Bonus</div></div><div class="gd-sim-grid"><button class="gd-sim-button red" data-side="Aaron" data-kind="goal" type="button">Aaron Goal</button><button class="gd-sim-button" data-side="Julie" data-kind="goal" type="button">Julie Goal</button><button class="gd-sim-button red" data-side="Aaron" data-kind="assist" type="button">Aaron Assist</button><button class="gd-sim-button" data-side="Julie" data-kind="assist" type="button">Julie Assist</button><button class="gd-sim-button red" data-side="Aaron" data-kind="first" type="button">Aaron First Goal</button><button class="gd-sim-button" data-side="Julie" data-kind="first" type="button">Julie First Goal</button></div><div class="gd-label-row"><div class="gd-label">Rivalry Feed</div><div class="gd-filter">Live</div></div><section class="gd-feed-list">${state.feed.map((item)=>`<article class="gd-card gd-feed-item"><div class="gd-feed-icon">${item.icon}</div><div><div><strong>${item.title}</strong></div><div class="gd-feed-sub">${item.detail}</div></div><div><strong>+${item.points}</strong></div></article>`).join('')}</section>`;}
  function renderFinal(){const state=getFinalData(), bonus=firstGoalHit(state.users); return `<section class="gd-card gd-postgame-card"><div class="gd-postgame-top"><div class="gd-postgame-icon">⭐</div><div><div class="gd-postgame-title">Postgame Summary</div><div class="gd-postgame-sub">How the night was won.</div></div></div><div class="gd-postgame-grid"><div class="gd-postgame-pill"><strong>MVP</strong><span>${mvpText(state.users)}</span></div><div class="gd-postgame-pill"><strong>Edge</strong><span>${leadingStatType(state.users)}</span></div><div class="gd-postgame-pill"><strong>Bonus</strong><span>${bonus?`${bonus.player} hit first goal bonus`:'First goal bonus not hit'}</span></div><div class="gd-postgame-pill"><strong>Total Events</strong><span>${totalGoals(state.users)} goals • ${totalAssists(state.users)} assists</span></div></div></section><div class="gd-label-row"><div class="gd-label">Final Pick Breakdown</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-final-picks">${renderPlayerCard('Aaron',state.users.Aaron,state.scores.Aaron,true)}${renderPlayerCard('Julie',state.users.Julie,state.scores.Julie,false)}</section>`;}
  function renderManageSheet(){const actions=$('#manageSheetActions'); if(!actions)return; const allSelected=()=>[...CR.gameDay.pregame.Aaron,...CR.gameDay.pregame.Julie]; actions.innerHTML=['Aaron','Julie'].flatMap((side)=>[0,1].map((index)=>{const selected=CR.gameDay.pregame[side][index]||''; const opts=[''].concat(roster.map((r)=>r.name)).map((name)=>{const disabled=name&&allSelected().includes(name)&&name!==selected; return `<option value="${name}" ${name===selected?'selected':''} ${disabled?'disabled':''}>${name||'Open slot'}</option>`;}).join(''); return `<div class="gd-sheet-pick"><strong>${side} Pick ${index+1}</strong><small>Swap locked player</small><select class="gd-sheet-select" data-side="${side}" data-index="${index}">${opts}</select></div>`;}).join('')).join(''); actions.querySelectorAll('.gd-sheet-select').forEach((select)=>{select.addEventListener('change',(event)=>{const side=event.target.dataset.side, index=Number(event.target.dataset.index), next=event.target.value, updated=CR.gameDay.pregame[side].slice(); updated[index]=next; CR.gameDay.pregame[side]=updated.filter(Boolean); renderManageSheet();});});}
  function bindInteractions(){document.querySelectorAll('.gd-small-action').forEach((button)=>{button.addEventListener('click',()=>{const side=button.dataset.side, player=button.dataset.player; CR.gameDay.pregame[side]=CR.gameDay.pregame[side].filter((name)=>name!==player); CR.renderGameDayState('pregame');});}); document.querySelectorAll('.gd-draft-btn').forEach((button)=>{button.addEventListener('click',(event)=>{event.preventDefault(); event.stopPropagation(); const player=button.dataset.player; if(claimedOwner(player))return; const total=CR.gameDay.pregame.Aaron.length+CR.gameDay.pregame.Julie.length; if(total>=4)return; const side=draftOrder[total]; CR.gameDay.pregame[side].push(player); CR.renderGameDayState('pregame');});}); document.querySelectorAll('[data-action="open-manage"]').forEach((button)=>{button.addEventListener('click',()=>{renderManageSheet(); setModalOpen(true);});}); document.querySelectorAll('.gd-sim-button').forEach((button)=>{button.addEventListener('click',()=>{const side=button.dataset.side, kind=button.dataset.kind, pick=CR.gameDay.live.users[side][0]; if(!pick)return; if(kind==='goal'){pick.goals+=1; CR.gameDay.live.scores[side]+=2; CR.gameDay.live.feed.unshift({icon:'🚨',title:`${pick.player} goal`,detail:`${side} scores through a picked player`,points:2});} if(kind==='assist'){pick.assists+=1; CR.gameDay.live.scores[side]+=1; CR.gameDay.live.feed.unshift({icon:'🍎',title:`${pick.player} assist`,detail:`${side} adds an assist point`,points:1});} if(kind==='first'&&!pick.firstGoal){pick.firstGoal=true; CR.gameDay.live.scores[side]+=2; CR.gameDay.live.feed.unshift({icon:'⭐',title:`${pick.player} first Canes goal`,detail:`${side} gets the first goal bonus`,points:2});} CR.flashSync?.(); CR.showToast?.(`${side} ${kind} update`); CR.renderGameDayState('live');});});}
  CR.renderGameDayState=(mode=CR.gameDay.mode)=>{CR.gameDay.mode=mode; const container=$('#gameDayContent'), view=$('#gameDayView'); if(!container||!view)return; view.classList.toggle('mode-playoffs',isPlayoffs()); container.innerHTML=`${renderHero()}${mode==='pregame'?renderPregame():''}${mode==='live'?renderLive():''}${mode==='final'?renderFinal():''}`; $('#stateTitle').textContent=mode==='pregame'?'Pregame':mode==='live'?'Live':'Final'; $('#stateBadge').textContent=isPlayoffs()?'Playoffs':(mode==='pregame'?'Regular':mode==='live'?'Live':'Final'); $('#stateSwitcher')?.querySelectorAll('button').forEach((button)=>button.classList.toggle('active',button.dataset.mode===mode)); $('#modeSwitcher')?.querySelectorAll('button').forEach((button)=>button.classList.toggle('active',button.dataset.playoffMode===CR.gameDay.playoffMode)); bindInteractions();};
  CR.initGameDay=()=>{$('#stateSwitcher')?.addEventListener('click',(event)=>{const button=event.target.closest('button[data-mode]'); if(button)CR.renderGameDayState(button.dataset.mode);}); $('#modeSwitcher')?.addEventListener('click',(event)=>{const button=event.target.closest('button[data-playoff-mode]'); if(!button)return; CR.gameDay.playoffMode=button.dataset.playoffMode; CR.renderGameDayState();}); $('#refreshButton')?.addEventListener('click',()=>{CR.flashSync?.(); CR.showToast?.('Mock realtime refresh complete');}); $('#closeSheet')?.addEventListener('click',()=>setModalOpen(false)); $('#saveSheet')?.addEventListener('click',()=>{setModalOpen(false); CR.renderGameDayState();}); $('#manageSheet')?.addEventListener('click',(event)=>{if(event.target.id==='manageSheet')setModalOpen(false);}); CR.renderGameDayState('pregame');};
})();