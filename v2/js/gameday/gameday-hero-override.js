window.CR=window.CR||{};(()=>{const r=window.CR.gameDayRender;if(!r)return;const orig=r.renderHeroSection;r.renderHeroSection=(args)=>{const{mode,pregameUsers,live,final,isPlayoffs,winnerText,nextDraftSide}=args;const pregame=mode==='pregame';const liveMode=mode==='live';const finalMode=mode==='final';const scores=pregame?{Aaron:0,Julie:0}:(finalMode?final.scores:live.scores);const period=pregame?'Tonight • 7:00 PM':(liveMode?live.period:'');const delta=scores.Aaron-scores.Julie;const momentum=Math.min(Math.abs(delta)*12,48);const momentumLeft=delta>0?`calc(50% - ${momentum}%)`:'50%';const subline=pregame?(nextDraftSide?`${nextDraftSide} drafting next • Pick ${pregameUsers.Aaron.length+pregameUsers.Julie.length+1} of 4`:'Picks ready for puck drop'):'';const leftBadge=finalMode?'Final':(liveMode?'Live':'Pregame');const playoffPill=isPlayoffs?`<span class="gd-pill gd-pill-playoff">Playoff Mode</span>`:'';const playoffSubline=isPlayoffs&&pregame?`<div class="gd-playoff-copy">Postseason stakes are up tonight.</div>`:'';const finalBanner=finalMode?`<div class="gd-final-banner ${isPlayoffs?'gd-final-banner-playoff':''}">${winnerText(scores)}</div>`:'';return `<section class="gd-hero ${finalMode?'gd-hero-final':''} ${isPlayoffs?'gd-hero-playoff':''}"><div class="gd-status-row-primary"><div class="gd-status-side gd-status-side-left"><span class="gd-pill gd-pill-state ${liveMode?'live':finalMode?'final':'pregame'}">${leftBadge}</span></div><div class="gd-status-side gd-status-side-right">${playoffPill}</div></div>${period?`<div class="gd-status-row-secondary"><div class="gd-status-center gd-status-center-secondary"><span class="gd-period">${period}</span></div></div>`:''}<div class="gd-score-grid"><div class="gd-side"><div class="gd-side-label red">Aaron</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Aaron.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Aaron}</div>`}</div><div class="gd-center"><img class="gd-logo ${isPlayoffs?'gd-logo-playoff':''}" src="./assets/app-icon.png" alt="Canes Rivalry"></div><div class="gd-side"><div class="gd-side-label">Julie</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Julie.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Julie}</div>`}</div></div>${subline?`<div class="gd-subline ${isPlayoffs?'gd-subline-playoff':''}">${subline}</div>`:''}${playoffSubline}${finalBanner}${liveMode?`<div class="gd-momentum-label ${isPlayoffs?'gd-momentum-label-playoff':''}">Momentum</div><div class="gd-track ${isPlayoffs?'gd-track-playoff':''}"><div class="gd-track-fill gd-momentum-fill ${isPlayoffs?'gd-track-fill-playoff':''}" style="left:${momentumLeft};width:${momentum}%"></div></div>`:''}</section>`;};})();