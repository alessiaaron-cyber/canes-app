window.CR=window.CR||{};
(()=>{
const CR=window.CR;
CR.gameDayRender={
renderStatChips(pick){return `<div class="gd-player-stats"><span class="gd-stat ${pick.goals?'live':''}">G ${pick.goals}</span><span class="gd-stat ${pick.assists?'live':''}">A ${pick.assists}</span><span class="gd-stat ${pick.firstGoal?'live':''}">FG</span></div>`;},
renderPlayerCard({side,picks,score,red,pointsFor,isPlayoffs}){const total=picks.reduce((sum,pick)=>sum+pointsFor(pick),0);return `<article class="gd-card gd-score-card ${isPlayoffs?'gd-card-playoff':''}"><div class="gd-pick-card-head"><strong class="${red?'red':''}">${side}</strong><span class="gd-pick-card-score">${total} pts</span></div>${picks.map((pick)=>`<div class="gd-player-card"><div class="gd-player-main"><strong>${pick.player}</strong>${CR.gameDayRender.renderStatChips(pick)}</div><div class="gd-player-total">+${pointsFor(pick)}</div></div>`).join('')}</article>`;},
renderHeroSection({mode,pregameUsers,live,final,isPlayoffs,winnerText,nextDraftSide}){const pregame=mode==='pregame';const liveMode=mode==='live';const finalMode=mode==='final';const scores=pregame?{Aaron:0,Julie:0}:(finalMode?final.scores:live.scores);const period=pregame?'Tonight • 7:00 PM':(liveMode?live.period:'');const delta=scores.Aaron-scores.Julie;const momentum=Math.min(Math.abs(delta)*12,48);const momentumLeft=delta>0?`calc(50% - ${momentum}%)`:'50%';const subline=pregame?(nextDraftSide?`${nextDraftSide} drafting next • Pick ${pregameUsers.Aaron.length+pregameUsers.Julie.length+1} of 4`:'Picks ready for puck drop'):'';const leftBadge=finalMode?'Final':(liveMode?'Live':'Pregame');const playoffPill=isPlayoffs?`<span class="gd-pill gd-pill-playoff">Playoff Mode</span>`:'';const playoffSubline=isPlayoffs&&pregame?`<div class="gd-playoff-copy">Postseason stakes are up tonight.</div>`:'';const finalBanner=finalMode?`<div class="gd-final-banner ${isPlayoffs?'gd-final-banner-playoff':''}">${winnerText(scores)}</div>`:'';return `<section class="gd-hero ${finalMode?'gd-hero-final':''} ${isPlayoffs?'gd-hero-playoff':''}"><div class="gd-status-row"><div class="gd-status-side gd-status-side-left"><span class="gd-pill gd-pill-state ${liveMode?'live':finalMode?'final':'pregame'}">${leftBadge}</span>${playoffPill}</div><div class="gd-status-center">${period?`<span class="gd-period">${period}</span>`:''}</div></div><div class="gd-score-grid"><div class="gd-side"><div class="gd-side-label red">Aaron</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Aaron.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Aaron}</div>`}</div><div class="gd-center"><img class="gd-logo ${isPlayoffs?'gd-logo-playoff':''}" src="./assets/app-icon.png" alt="Canes Rivalry"></div><div class="gd-side"><div class="gd-side-label">Julie</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Julie.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Julie}</div>`}</div></div>${subline?`<div class="gd-subline ${isPlayoffs?'gd-subline-playoff':''}">${subline}</div>`:''}${playoffSubline}${finalBanner}${liveMode?`<div class="gd-momentum-label ${isPlayoffs?'gd-momentum-label-playoff':''}">Momentum</div><div class="gd-track ${isPlayoffs?'gd-track-playoff':''}"><div class="gd-track-fill gd-momentum-fill ${isPlayoffs?'gd-track-fill-playoff':''}" style="left:${momentumLeft};width:${momentum}%"></div></div>`:''}</section>`;},
renderPregameSection({users,roster,claimedOwner,isPlayoffs}){const lastDrafted=CR.gameDay?.lastDraftedPlayer||'';const renderOwner=(side,red)=>`<article class="gd-panel ${isPlayoffs?'gd-panel-playoff':''}"><div class="gd-panel-head ${red?'red':'dark'} ${isPlayoffs?'gd-panel-head-playoff':''}"><span>${side}</span><span>${users[side].length}/2</span></div>${[0,1].map((index)=>{const pick=users[side][index];const isFocus=pick&&pick.player===lastDrafted;return pick?`<div class="gd-pick-row ${isFocus?'gd-pick-row-focus':''}" data-pick-side="${side}" data-pick-player="${pick.player}"><div class="gd-pick-icon">✓</div><div class="gd-pick-main"><strong>${pick.player}</strong><small>${isPlayoffs?'Locked for playoff night':'Locked pick'}</small><div class="gd-lock-actions"><button class="gd-small-action" data-side="${side}" data-player="${pick.player}" type="button">Change</button></div></div></div>`:`<div class="gd-pick-row is-empty"><div class="gd-pick-icon">…</div><div class="gd-pick-main"><strong>Open slot</strong><small>${isPlayoffs?'Waiting for the next playoff pick':'Waiting for next pick'}</small></div></div>`;}).join('')}</article>`;return `<div class="gd-label-row" id="gdPregamePicksAnchor"><div class="gd-label">${isPlayoffs?'Playoff Picks':'Picks'}</div></div><section class="gd-picks-grid" id="gdPregamePicksGrid">${renderOwner('Aaron',true)}${renderOwner('Julie',false)}</section><div class="gd-label-row"><div class="gd-label">Current Canes Roster</div></div><section class="gd-panel gd-roster ${isPlayoffs?'gd-panel-playoff':''}">${roster.map((entry)=>{const owner=claimedOwner(entry.name);return `<div class="gd-roster-row ${owner?'claimed':''}"><div class="gd-pick-main"><strong>${entry.name}</strong><small>${entry.detail}</small></div>${owner?`<span class="gd-tag">${owner}</span>`:`<button class="gd-draft-btn ${isPlayoffs?'gd-draft-btn-playoff':''}" data-player="${entry.name}" type="button">Draft</button>`}</div>`;}).join('')}</section>`;},
renderLiveSection({state,renderPlayerCard,carryover,isPlayoffs}){return `<div class="gd-label-row"><div class="gd-label-group"><div class="gd-label">${isPlayoffs?'Playoff Picks':'Picked Players'}</div>${carryover?.active?`<span class="gd-inline-note gd-inline-note-warning">Carryover</span>`:''}${isPlayoffs?`<span class="gd-inline-note gd-inline-note-playoff">High Stakes</span>`:''}</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-picks-grid">${renderPlayerCard({side:'Aaron',picks:state.users.Aaron,score:state.scores.Aaron,red:true,isPlayoffs})}${renderPlayerCard({side:'Julie',picks:state.users.Julie,score:state.scores.Julie,red:false,isPlayoffs})}</section><div class="gd-label-row"><div class="gd-label">Simulate Updates</div><div class="gd-filter">${isPlayoffs?'Playoff Moment Lab':'Goal / Assist / Bonus'}</div></div><div class="gd-sim-grid"><button class="gd-sim-button red ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Aaron" data-kind="goal" type="button">Aaron Goal</button><button class="gd-sim-button ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Julie" data-kind="goal" type="button">Julie Goal</button><button class="gd-sim-button red ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Aaron" data-kind="assist" type="button">Aaron Assist</button><button class="gd-sim-button ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Julie" data-kind="assist" type="button">Julie Assist</button><button class="gd-sim-button red ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Aaron" data-kind="first" type="button">Aaron First Goal</button><button class="gd-sim-button ${isPlayoffs?'gd-sim-button-playoff':''}" data-side="Julie" data-kind="first" type="button">Julie First Goal</button></div><div class="gd-label-row"><div class="gd-label">${isPlayoffs?'Playoff Rivalry Feed':'Rivalry Feed'}</div></div><section class="gd-feed-list">${state.feed.map((item,index)=>`<article class="gd-card gd-feed-item gd-feed-tier-${item.tier||'light'} ${index===0?'gd-feed-item-latest':''} ${isPlayoffs?'gd-feed-item-playoff':''}"><div class="gd-feed-icon">${item.icon}</div><div class="gd-feed-main"><strong>${item.title}</strong><div class="gd-feed-sub">${item.detail}</div></div><div class="gd-feed-points">+${item.points}</div></article>`).join('')}</section>`;},
renderFinalSection({state,bonusText,mvpText,edgeText,totalEventsText,renderPlayerCard,carryover,isPlayoffs}){return `<section class="gd-card gd-postgame-card ${isPlayoffs?'gd-postgame-card-playoff':''}"><div class="gd-postgame-top"><div class="gd-postgame-icon ${isPlayoffs?'gd-postgame-icon-playoff':''}">🏆</div><div><div class="gd-postgame-title">${isPlayoffs?'Playoff Summary':'Postgame Summary'}</div><div class="gd-postgame-sub">${isPlayoffs?'How the playoff night swung the rivalry.':'How the night was won.'}</div></div></div><div class="gd-postgame-grid"><div class="gd-postgame-pill"><strong>MVP</strong><span>${mvpText}</span></div><div class="gd-postgame-pill"><strong>Edge</strong><span>${edgeText}</span></div><div class="gd-postgame-pill"><strong>Bonus</strong><span>${bonusText}</span></div><div class="gd-postgame-pill"><strong>${isPlayoffs?'Playoff Events':'Total Events'}</strong><span>${totalEventsText}</span></div></div></section><div class="gd-label-row"><div class="gd-label-group"><div class="gd-label">${isPlayoffs?'Playoff Pick Breakdown':'Final Pick Breakdown'}</div>${carryover?.active?`<span class="gd-inline-note gd-inline-note-warning">Carryover</span>`:''}${isPlayoffs?`<span class="gd-inline-note gd-inline-note-playoff">Playoffs</span>`:''}</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-final-picks">${renderPlayerCard({side:'Aaron',picks:state.users.Aaron,score:state.scores.Aaron,red:true,isPlayoffs})}${renderPlayerCard({side:'Julie',picks:state.users.Julie,score:state.scores.Julie,red:false,isPlayoffs})}</section>`;}};
})();