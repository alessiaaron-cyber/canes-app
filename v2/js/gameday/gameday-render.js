window.CR=window.CR||{};
(()=>{
const CR=window.CR;
CR.gameDayRender={
renderStatChips(pick){return `<div class="gd-player-stats"><span class="gd-stat ${pick.goals?'live':''}">G ${pick.goals}</span><span class="gd-stat ${pick.assists?'live':''}">A ${pick.assists}</span><span class="gd-stat ${pick.firstGoal?'live':''}">FG</span></div>`;},
renderPlayerCard({side,picks,score,red,pointsFor}){const total=picks.reduce((sum,pick)=>sum+pointsFor(pick),0);return `<article class="gd-card gd-score-card"><div class="gd-pick-card-head"><strong class="${red?'red':''}">${side}</strong><span class="gd-pick-card-score">${total} pts</span></div>${picks.map((pick)=>`<div class="gd-player-card"><div class="gd-player-main"><strong>${pick.player}</strong>${CR.gameDayRender.renderStatChips(pick)}</div><div class="gd-player-total">+${pointsFor(pick)}</div></div>`).join('')}</article>`;},
renderHeroSection({mode,pregameUsers,live,final,isPlayoffs,winnerText,nextDraftSide}){const pregame=mode==='pregame';const liveMode=mode==='live';const finalMode=mode==='final';const scores=pregame?{Aaron:0,Julie:0}:(finalMode?final.scores:live.scores);const period=pregame?'Tonight • 7:00 PM':(liveMode?live.period:'');const delta=scores.Aaron-scores.Julie;const momentum=Math.min(Math.abs(delta)*12,48);const momentumLeft=delta>0?`calc(50% - ${momentum}%)`:'50%';const subline=pregame?(nextDraftSide?`${nextDraftSide} drafting next • Pick ${pregameUsers.Aaron.length+pregameUsers.Julie.length+1} of 4`:'Picks ready for puck drop'):'';const leftBadge=finalMode?'Final':(liveMode?'Live':'Pregame');return `<section class="gd-hero ${finalMode?'gd-hero-final':''}"><div class="gd-status-row"><div class="gd-status-side gd-status-side-left"><span class="gd-pill gd-pill-state ${liveMode?'live':finalMode?'final':'pregame'}">${leftBadge}</span></div><div class="gd-status-center">${period?`<span class="gd-period">${period}</span>`:''}</div></div><div class="gd-score-grid"><div class="gd-side"><div class="gd-side-label red">Aaron</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Aaron.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Aaron}</div>`}</div><div class="gd-center"><img class="gd-logo" src="./assets/app-icon.png" alt="Canes Rivalry"></div><div class="gd-side"><div class="gd-side-label">Julie</div>${pregame?`<div class="gd-pregame-count">${pregameUsers.Julie.length}<span class="gd-pregame-total">/2</span></div><div class="gd-pregame-meta">Picks Locked</div>`:`<div class="gd-side-value gd-score-pop">${scores.Julie}</div>`}</div></div>${subline?`<div class="gd-subline">${subline}</div>`:''}${finalMode?`<div class="gd-final-banner">${winnerText(scores)}</div>`:''}${liveMode?`<div class="gd-momentum-label">Momentum</div><div class="gd-track"><div class="gd-track-fill gd-momentum-fill" style="left:${momentumLeft};width:${momentum}%"></div></div>`:''}</section>`;},
renderPregameSection({users,roster,claimedOwner}){const lastDrafted=CR.gameDay?.lastDraftedPlayer||'';const renderOwner=(side,red)=>`<article class="gd-panel"><div class="gd-panel-head ${red?'red':'dark'}"><span>${side}</span><span>${users[side].length}/2</span></div>${[0,1].map((index)=>{const pick=users[side][index];const isFocus=pick&&pick.player===lastDrafted;return pick?`<div class="gd-pick-row ${isFocus?'gd-pick-row-focus':''}" data-pick-side="${side}" data-pick-player="${pick.player}"><div class="gd-pick-icon">✓</div><div class="gd-pick-main"><strong>${pick.player}</strong><small>Locked pick</small><div class="gd-lock-actions"><button class="gd-small-action" data-side="${side}" data-player="${pick.player}" type="button">Change</button></div></div></div>`:`<div class="gd-pick-row is-empty"><div class="gd-pick-icon">…</div><div class="gd-pick-main"><strong>Open slot</strong><small>Waiting for next pick</small></div></div>`;}).join('')}</article>`;return `<div class="gd-label-row" id="gdPregamePicksAnchor"><div class="gd-label">Picks</div></div><section class="gd-picks-grid" id="gdPregamePicksGrid">${renderOwner('Aaron',true)}${renderOwner('Julie',false)}</section><div class="gd-label-row"><div class="gd-label">Current Canes Roster</div></div><section class="gd-panel gd-roster">${roster.map((entry)=>{const owner=claimedOwner(entry.name);return `<div class="gd-roster-row ${owner?'claimed':''}"><div class="gd-pick-main"><strong>${entry.name}</strong><small>${entry.detail}</small></div>${owner?`<span class="gd-tag">${owner}</span>`:`<button class="gd-draft-btn" data-player="${entry.name}" type="button">Draft</button>`}</div>`;}).join('')}</section>`;},
renderLiveSection({state,renderPlayerCard,carryover}){return `<div class="gd-label-row"><div class="gd-label-group"><div class="gd-label">Picked Players</div>${carryover?.active?`<span class="gd-inline-note gd-inline-note-warning">Carryover</span>`:''}</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-picks-grid">${renderPlayerCard({side:'Aaron',picks:state.users.Aaron,score:state.scores.Aaron,red:true})}${renderPlayerCard({side:'Julie',picks:state.users.Julie,score:state.scores.Julie,red:false})}</section><div class="gd-label-row"><div class="gd-label">Simulate Updates</div><div class="gd-filter">Goal / Assist / Bonus</div></div><div class="gd-sim-grid"><button class="gd-sim-button red" data-side="Aaron" data-kind="goal" type="button">Aaron Goal</button><button class="gd-sim-button" data-side="Julie" data-kind="goal" type="button">Julie Goal</button><button class="gd-sim-button red" data-side="Aaron" data-kind="assist" type="button">Aaron Assist</button><button class="gd-sim-button" data-side="Julie" data-kind="assist" type="button">Julie Assist</button><button class="gd-sim-button red" data-side="Aaron" data-kind="first" type="button">Aaron First Goal</button><button class="gd-sim-button" data-side="Julie" data-kind="first" type="button">Julie First Goal</button></div><div class="gd-label-row"><div class="gd-label">Rivalry Feed</div></div><section class="gd-feed-list">${state.feed.map((item,index)=>`<article class="gd-card gd-feed-item gd-feed-tier-${item.tier||'light'} ${index===0?'gd-feed-item-latest':''}"><div class="gd-feed-icon">${item.icon}</div><div class="gd-feed-main"><strong>${item.title}</strong><div class="gd-feed-sub">${item.detail}</div></div><div class="gd-feed-points">+${item.points}</div></article>`).join('')}</section>`;},
renderFinalSection({state,bonusText,mvpText,edgeText,totalEventsText,renderPlayerCard,carryover}){return `<section class="gd-card gd-postgame-card"><div class="gd-postgame-top"><div class="gd-postgame-icon">🏆</div><div><div class="gd-postgame-title">Postgame Summary</div><div class="gd-postgame-sub">How the night was won.</div></div></div><div class="gd-postgame-grid"><div class="gd-postgame-pill"><strong>MVP</strong><span>${mvpText}</span></div><div class="gd-postgame-pill"><strong>Edge</strong><span>${edgeText}</span></div><div class="gd-postgame-pill"><strong>Bonus</strong><span>${bonusText}</span></div><div class="gd-postgame-pill"><strong>Total Events</strong><span>${totalEventsText}</span></div></div></section><div class="gd-label-row"><div class="gd-label-group"><div class="gd-label">Final Pick Breakdown</div>${carryover?.active?`<span class="gd-inline-note gd-inline-note-warning">Carryover</span>`:''}</div><button class="gd-manage-tiny" data-action="open-manage" type="button">Manage</button></div><section class="gd-final-picks">${renderPlayerCard({side:'Aaron',picks:state.users.Aaron,score:state.scores.Aaron,red:true})}${renderPlayerCard({side:'Julie',picks:state.users.Julie,score:state.scores.Julie,red:false})}</section>`;}};
})();